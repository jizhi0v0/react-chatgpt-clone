import { createContext} from "react";

export const ChatsContext = createContext(null);
export const ChatsActionContext = createContext(null);
export const SetRenderingChatIdContext = createContext(null);
